# ============================================================
# PhishGuard Backend – Python dependencies
# Pinned for Python 3.11 (Docker).  Locally on Python 3.13
# the resolver will pick compatible newer versions automatically.
# ============================================================

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Data validation
pydantic>=2.7.1

# ML core
scikit-learn>=1.4.2
xgboost>=2.0.3
joblib>=1.4.0
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0

# Text processing
nltk>=3.8.1

# Explainability (optional but recommended)
shap>=0.45.0

# Utilities
tqdm>=4.66.0
requests>=2.32.0

# Development / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0           # needed by TestClient

# Code quality (dev only – can remove in production image)
flake8>=7.0.0
